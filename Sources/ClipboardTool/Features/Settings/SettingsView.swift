import SwiftUI

// Native macOS Settings window with sidebar navigation.
// Implementation tracked in issue #13.
struct SettingsView: View {
    var body: some View {
        // TODO: implement — issue #13
        Text("Settings")
            .frame(width: 520, height: 360)
    }
}
